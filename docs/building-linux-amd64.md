# Build instructions for Linux amd64

## Install third party software

- Install **Avahi compat lib**: `sudo apt-get install libavahi-compat-libdnssd-dev`

## Clone source code

`git clone --recursive https://github.com/noseam-env/flowdrop-cli`
